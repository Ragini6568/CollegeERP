import { makeStyles } from "@mui/styles";

const useStyles = makeStyles({
  form: {
    width: "70%",
    padding: "4rem",
    margin: "50px auto",
    textAlign: "center",
  },

  button: {
    marginTop: "3rem",
  },

  textfield: {
    marginTop: "1rem",
  },

  buttonGrp: {
    marginTop: "2rem",
    textAlign: "center",
  },
});

export default useStyles;
